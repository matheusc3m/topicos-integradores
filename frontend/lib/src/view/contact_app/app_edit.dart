import 'package:flutter/material.dart';

class AppEdit extends StatelessWidget {
  const AppEdit({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Todo"),
    );
  }
}
